CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    username VARCHAR2(100),
    email VARCHAR2(100),
    phone VARCHAR2(15)
);

CREATE TABLE categories (
    id INTEGER PRIMARY KEY,
    name VARCHAR2(50),
    description VARCHAR2(250)
);

CREATE TABLE products (
    id INTEGER PRIMARY KEY,
    name VARCHAR2(100),
    description CLOB,
    price NUMBER(10, 2),
    category_id INTEGER,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);

CREATE TABLE reviews (
    id INTEGER PRIMARY KEY,
    product_id INTEGER,
    reviewer_id INTEGER,
    rating INTEGER,
    text_review VARCHAR2(255),
    FOREIGN KEY (product_id) REFERENCES products(id),
    FOREIGN KEY (reviewer_id) REFERENCES users(id)
);

CREATE TABLE user_preferences (
    user_id INTEGER PRIMARY KEY,
    feature VARCHAR2(100),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE recommendations (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    product_id INTEGER,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);

CREATE TABLE purchase_history (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    product_id INTEGER,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);

CREATE TABLE product_features (
    product_id INTEGER PRIMARY KEY,
    feature VARCHAR2(100),
    FOREIGN KEY (product_id) REFERENCES products(id)
);
==========================
CREATE SEQUENCE purchase_history_seq
    START WITH 1
    INCREMENT BY 1
    NOCACHE
    NOCYCLE;
==========================
auto inc:
CREATE SEQUENCE users_id_seq
START WITH 1 
INCREMENT BY 1 
NOMAXVALUE 
NOCYCLE;

ALTER TABLE users
MODIFY id INTEGER DEFAULT users_id_seq.NEXTVAL;
==========================
edit feature
CREATE TABLE features (
    id INTEGER PRIMARY KEY,
    feature VARCHAR2(100) NOT NULL UNIQUE
);
==
ALTER TABLE user_preferences
DROP COLUMN feature;

ALTER TABLE user_preferences
ADD feature_id INTEGER;

ALTER TABLE user_preferences
ADD CONSTRAINT fk_user_preferences_feature
FOREIGN KEY (feature_id)
REFERENCES features(id);
==
ALTER TABLE product_features
DROP COLUMN feature;

ALTER TABLE product_features
ADD feature_id INTEGER;

ALTER TABLE product_features
ADD CONSTRAINT fk_product_features_feature
FOREIGN KEY (feature_id)
REFERENCES features(id);
==========================
C/app/Mateu/product/21c/
SYS,SYSTEM,PDBADMIN -> password: 1324

cmd -> sqlplus -> username: sys as sysdba ->password: 1324
alter session set "_ORACLE_SCRIPT"=true;
user: mat pass: 1324
grant all privileges to mat;
==========================
to check connection parameters:
cmd->lsnrctl status: xepdb1 to baza danych

services.msc -> wl listenera
